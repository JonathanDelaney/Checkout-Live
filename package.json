{
  "name": "checkout-live",
  "lockfileVersion": 2,
  "requires": true,
  "packages": {},
  "scripts": {    
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./server/app"
  }
}
