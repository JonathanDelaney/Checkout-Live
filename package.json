{
  "name": "checkout-live",
  "lockfileVersion": 2,
  "requires": true,
  "packages": {},
  "scripts": {
      "start": "npm run devserver",
      "test": "echo \"Error: no test specified\" && exit 1",
      "devserver": "node ./server/node/index.js"
  }
}
