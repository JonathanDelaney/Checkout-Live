{
  "name": "checkout-components-sample-code",
  "lockfileVersion": 2,
  "requires": true,
  "packages": {},
  "scripts": {    
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node run devserver",
    "devserver": "node ./server/node/index.js"
  }
}
