{
  "name": "checkout-live",
  "lockfileVersion": 2,
  "requires": true,
  "packages": {},
  "scripts": {
    "start": "node ./server/app",
    "start": "ng serve --proxy-config ./client/proxy.conf.json"
  }
}
